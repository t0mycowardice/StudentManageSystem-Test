学生选课管理系统 软件测试报告
===============

一、 测试基本信息
---------

* **项目名称**：学生选课管理系统

* **测试人员**：[你的名字]

* **测试时间**：2026年9月

* **测试类型**：功能测试、接口测试、性能测试、安全测试、自动化测试

二、 测试环境
-------

* **操作系统**：Windows 11

* **浏览器**：Google Chrome / Microsoft Edge

* **后端环境**：JDK 11、Spring Boot 2.6.3、MySQL 8.0

* **前端环境**：Node.js、Vue 2.x

* **测试工具**：
  
  * 测试管理：Xmind（测试点梳理）、Excel（测试用例编写）
  
  * 接口测试：Postman
  
  * 性能测试：Apache JMeter
  
  * 自动化测试：Python 3.14 + Pytest + Selenium

三、 测试范围与策略
----------

本次测试针对系统的核心业务模块（登录、学生选课、教师成绩管理、管理员课程管理），采用**黑盒测试**为主，结合**灰盒测试**（API接口调用与数据库校验），重点验证系统的功能正确性、接口安全性及基础性能。
四、 测试执行与结果统计
------------

### 1. 功能测试

* **测试用例执行情况**：共设计并执行功能测试用例约 50 条，覆盖等价类、边界值及场景法。

* **执行结果**：核心流程（如正常登录、正常选课）通过，异常场景（如空值、非法字符、重复提交）基本能正确拦截。

* **发现缺陷**：发现 UI/功能建议类缺陷 3 个（如登录页存在未移除的 `Test` 调试按钮、部分提示语不友好等），均已在测试记录中标记。

### 2. 接口与安全测试（核心亮点）

* **工具**：Postman

* **测试动作**：抓取并验证教师登录、学生登录、查询用户信息等核心接口。

* **发现严重缺陷 1（未授权访问漏洞 - 致命）**：
  
  * _复现步骤_：使用 Postman 发送 `GET` 请求至 `/teacher/findById/6`，无需携带任何 Token 或 Cookie。
  
  * _实际结果_：接口直接返回管理员明文信息 `{"tid": 6, "tname": "admin", "password": "123"}`。
  
  * _预期结果_：应返回 401 未授权或 403 禁止访问。

* **发现严重缺陷 2（垂直越权漏洞 - 严重）**：
  
  * _复现步骤_：以学生身份登录系统，按 F12 打开开发者工具，在 `sessionStorage` 中手动将 `type` 字段由 `student` 修改为 `admin`，刷新页面。
  
  * _实际结果_：前端路由直接放行，学生账号成功进入管理员后台页面。
  
  * _预期结果_：后端应校验用户真实角色权限，无权访问应拦截并跳回登录页。

### 3. 性能测试

* **工具**：JMeter

* **测试场景**：针对教师登录接口进行 20 个并发用户的压力测试，循环 5 次（总样本数 100）。

* **测试结果**：错误率为 **0%**，平均响应时间在 50ms 以内，吞吐量稳定。

* **结论**：系统在低并发场景下具备良好的稳定性，无内存泄漏或服务崩溃现象。

### 4. 自动化测试

* **框架**：Python + Pytest + Selenium，采用 Page Object（PO）设计模式。

* **覆盖模块**：登录模块（正向用例、逆向用例）。

* **执行结果**：脚本运行正常，断言全部通过，输出了自动化测试执行日志。

五、 缺陷总结与测试结论
------------

* **缺陷汇总**：共发现缺陷 5 个（致命 1 个，严重 1 个，一般 3 个）。

* **整体评价**：  
  该系统在**功能逻辑**上基本能够满足学生选课管理的基础业务需求，但在**前后端交互安全性**上存在致命短板。前端硬编码了管理员判定逻辑，后端接口完全缺乏鉴权机制，这会导致严重的数据泄露和越权风险。

* **测试结论**：**当前版本不建议在生产环境上线**。建议开发团队修复接口鉴权、加密敏感数据，并移除前端调试代码后，再进行回归测试。
